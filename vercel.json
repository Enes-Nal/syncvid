{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "cd ../.. && npm install --include=dev --workspaces --include-workspace-root",
  "buildCommand": "ROOT=\"$PWD\"; cd ../.. && npm run build -w @syncweb/web && REPO=\"$PWD\" && if [ \"$ROOT\" != \"$REPO/apps/web\" ]; then mkdir -p \"$ROOT/dist\" && cp -R \"$REPO/apps/web/dist/.\" \"$ROOT/dist/\"; fi",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
